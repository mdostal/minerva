# Q — Success metric
**Primary number: ≥3 ideas in flight concurrently, each progressing idea→spec with ZERO
hand-run commands per idea** (true async drop-in — I feed ideas, they plan themselves in
parallel, I only answer the gate questions).
**Secondary: idea-drop → approved-epic latency** — unattended except the human answer-gates.
It's working when I can drop several ideas and walk away, and get back approved epics without
having SSH'd in to run a single command per idea.
